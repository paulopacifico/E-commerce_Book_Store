# Spartan UI Parts Todo

- [x] Install Spartan dependencies (`@spartan-ng/brain`, `@spartan-ng/cli`)
- [x] Add Spartan Tailwind preset import (`@spartan-ng/brain/hlm-tailwind-preset.css`)
- [x] Configure Spartan project file (`components.json`)
- [x] Generate `button` Helm component
- [x] Generate `input` Helm component
- [x] Generate `dialog` Helm component
- [x] Generate `select` Helm component
- [x] Generate `table` Helm component
- [x] Generate `form-field` Helm component (form building block)

## Next Recommended Steps

- [ ] Add a shared UI barrel in app code that re-exports frequently used Helm imports.
- [ ] Build a demo page that uses all generated parts in one flow (form + dialog + table).
- [ ] Add dark mode toggle for `.dark` class to validate theme parity.
